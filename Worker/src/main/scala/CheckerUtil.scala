import org.slf4j.LoggerFactory

import java.io._

object CheckerUtil {
  // def checkSorted(mf: MultiFileRead): Boolean = {
  //   var recOpt = mf.readOneRecord
  //   while (recOpt != None) {
  //     val rec = recOpt.get
      
  //   }
  // }
}